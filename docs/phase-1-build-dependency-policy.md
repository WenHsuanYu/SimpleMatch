# Phase 1 Build and Dependency Policy Gate

Phase 1 completed the build-policy refactor without changing service behavior or the declared resolved versions.

## Completed Checkpoints

- `cab0c94`: moved pinned plugin and library versions to the version catalog.
- `62ff3be`: added and proved the Spring service convention with persistence.
- `53a74c1`: migrated account, risk, and QuickFIX services.
- `77a98f2`: centralized protobuf and gRPC Java generation.
- `bb1d26d`: made Flyway service identity authoritative while preserving legacy root task aliases.
- `0fa6b75`: moved Java and quality policy from root path predicates into conventions.
- This closure commit: enabled dependency locking and recorded its operating procedure.

## Gate Evidence

The catalog retains the prior declared versions. No version correction was intended or made during Phase 1. All Java
modules and the included build now use checked-in dependency locks; the root and included build also retain the
catalog-resolution `settings-gradle.lockfile` generated by Gradle.

The following commands passed against the locked graph:

```bash
./gradlew -q test :services:quickfix-gateway:certificationTest
./gradlew -q -p build-logic test
./gradlew -q staticAnalysis
./gradlew -q tasks --all
```

Flyway task discovery reported all 18 established root aliases: six actions for each of `accountService`, `persistence`,
and `riskService`.

## Rollback

Each Phase 1 checkpoint is independently committed. Revert service convention migrations before the root-policy
extraction if a module-specific rollback is needed; no service runtime behavior or database migration content changed.
