# Deployment Test Lessons

This is the living checklist for Compose, Docker, kind, and Kubernetes deployment tests. It records
repeatable mistakes and their prevention checks so a later run does not rediscover them.

## Run preflight

Before applying deployment resources or injecting a deployment fault:

1. Read the lessons below and run every prevention check that applies to the selected profile.
2. Confirm the Docker daemon, canonical kind cluster, Kubernetes context, namespace, and expected
   worker topology before selecting any Pod, Node, container, image, PVC, or topic.
3. Resolve runtime identities from the deployed artifact and API objects. Do not substitute a unit
   test fixture, expected ordinal, or remembered symbol for an observed assignment.
4. Confirm that the run has an isolated namespace and clean, run-owned evidence directory. Reuse
   prior phase evidence only when the runner's resume contract verifies the same source, cluster,
   trading day, and namespace.
5. Inventory Docker resources and generated caches before and after the run. Cleanup must preserve
   active resources and remove only exact run-owned disposable resources.

If a preflight check fails, stop before fault injection and record the result as an environment or
precondition failure. Do not make the test green by changing Kafka offsets, deleting authoritative
data, weakening fail-closed behavior, or guessing a replacement target.

## Recurring lessons

| ID | Symptom | Root cause | Prevention check | Safe fix | Last verified |
| --- | --- | --- | --- | --- | --- |
| DT-001 | Matching reports `kUnknownInstrument` during deployed E2E | A unit-test instrument such as `XTAI/2330` was sent to a partition that did not own it | Read the approved artifact's `routingPolicy.assignments` and select an instrument whose `partitionId` equals the target partition; assert the same value in the helper | Fix the E2E target selection. Preserve the Matching core's rejection and fail-closed behavior | 2026-08-15 |
| DT-002 | Kubernetes commands fail, or the API refuses connections | Docker is stopped, the kind cluster is absent, or the active context is not canonical | Run `docker info`, verify `kind-simplematch-live`, verify the current context, and verify one control plane plus three labelled workers before deployment work | Restore the daemon or select the verified canonical context; do not recreate or delete resources during a failed preflight | 2026-08-15 |
| DT-003 | A failed E2E contaminates later replay or makes a corrected test fail immediately | The first run wrote invalid records to the authoritative Matching topic and the consumer reused its stored offset | Before rerun, check the topic/run isolation and consumer boundary; never reuse a poisoned run as if it were clean | Create a fresh run-owned E2E environment or use the runner's verified resume path; do not edit offsets manually | 2026-08-15 |
| DT-004 | Matching fleet evidence is inconsistent with the intended topology | A StatefulSet was temporarily scaled, a debug Pod remained, or a PVC/PV was selected by name alone | Require 15/15 Ready Pods, unique Pod indexes, exact Pod UID to Node to PVC to PV mapping, and no unowned debug workload before E2E | Restore the run-owned StatefulSet and remove only the exact run-owned debug resource, then recapture the baseline | 2026-08-15 |
| DT-005 | Native configure or image build is killed by the host | Parallel compilation exceeds the local memory budget | Check available Docker/host memory and use the documented bounded parallelism for the selected preset before starting the build | Lower build parallelism or adjust the local resource budget; do not misclassify exit 137 as a source failure | 2026-08-15 |
| DT-006 | Cleanup removes useful state or leaves disk usage unexplained | A blanket prune was used without checking references and generated caches | Inventory container references, image IDs, volume ownership, builder cache, and generated build directories before cleanup | Remove exact stopped/run-owned resources and stale duplicate tags only when unreferenced; verify remaining inventory and reclaimed space | 2026-08-15 |
| DT-007 | Deployed Matching exits with SIGSEGV during OpenSSL cleanup | The binary used static vcpkg OpenSSL while system libcurl brought a separate dynamic OpenSSL dependency into the same process | For the Matching image, verify the curl dependency is resolved through the same vcpkg dependency graph and inspect `ldd /app/simplematch-matching` before deployment | Build curl through vcpkg, use config-mode CURL discovery, and remove the system libcurl development/runtime packages from the Matching image | 2026-08-15 |
| DT-008 | Process-crash E2E reports no restart after an apparently successful `kubectl exec kill` | The restricted Matching container did not expose a reliable in-container signal path for terminating PID 1; the fault command returned success without an observed container restart | Resolve the exact Pod UID, Node, and running container ID from Kubernetes and the matching kind worker before injecting the fault; require restart-count evidence afterward | Use the worker's CRI client to stop only that exact container with zero graceful-stop timeout, then verify the same Pod UID returns Ready and replays | 2026-08-15 |
| DT-009 | A run-owned Kubernetes helper cannot write its report under `/tmp` | The helper used a read-only root filesystem but the test Pod did not mount a writable scratch volume | For every run-owned helper, check its declared writable report directory before copying or executing the test binary | Mount a run-owned `emptyDir` at `/tmp`; keep the application workloads read-only and remove the exact helper after the run | 2026-08-15 |
| DT-010 | PostgreSQL and dependent workloads enter CrashLoopBackOff after Docker/kind recovery | The Docker Desktop data disk was full, so the kind workers could not write local-path PVC or runtime files; local-path PVC requested sizes are logical reservations, not preallocated per-volume capacity | Before deployment or fault injection, verify usable space on each kind worker's `/var` filesystem and retain a documented safety margin; also compare the aggregate local-path PVC request envelope with the Docker disk-image capacity | Expand the Docker disk image or remove only inventoried stale Docker resources, then wait for the full baseline to recover before resuming tests | 2026-08-15 |
| DT-011 | A deployed E2E report appears to prove recovery but cannot identify whether events belong to this run | The helper measured batch elapsed time and accepted any consumed event; the collector did not require full-fleet or Node/PVC/PV continuity evidence | Decode each Matching event, correlate it to a submitted `source_command_id`, record the per-event measurement definition, and reject metrics without all 15 runtime snapshots and the selected target's same-Node/PVC/PV identity | Strengthen the helper and collector before rerunning only the E2E phase; do not reinterpret an older report as proof of the new evidence contract | 2026-08-15 |
| DT-012 | A newly created kind control plane never becomes healthy and kubelet reports `overlay ... invalid argument` while creating Pod sandboxes | Docker's relocated data root was on an NTFS filesystem; images remained readable, but nested containerd overlay mounts used by kind could not be created reliably | Before creating kind, verify `docker info` storage root and `findmnt -T <DockerRootDir>`; require a Linux filesystem such as ext4 for the Docker data root and verify a disposable nested container before deployment | Move Docker data to a Docker Desktop-supported Linux VM disk or Linux filesystem, restart the daemon, delete only the failed canonical cluster, and rerun the repository cluster preflight | 2026-08-15 |
| DT-013 | Deployed E2E stops before injecting a fault with a valid run-token error | The checked-out shell harness required eight hexadecimal characters, but the existing native helper binary was stale and still enforced the former six-character contract | Before E2E, rebuild `simplematch-matching-e2e-certification` from the selected native build tree and verify the helper error/contract text agrees with the shell harness; treat a contract mismatch as a build-artifact failure | Rebuild only the helper target, then rerun only the E2E phase against the unchanged deployed namespace; do not alter Kafka offsets or fault targets | 2026-08-15 |
| DT-014 | A kind helper Pod reports `/bin/sh: exec format error` after Docker data relocation | The relocated Docker image store retained a platform-resolution state that was not usable by the nested kind/containerd runtime, even though the image metadata appeared to be amd64 | Before deployment, verify the helper image can execute a minimal `/bin/sh -c true` on every canonical worker and verify the exact local image is exportable as `linux/amd64` | Rebuild or flatten only the exact helper image from its verified amd64 child manifest, load it into the canonical cluster, and rerun the affected phase; do not reset the Docker store or prune the cluster | 2026-08-15 |
| DT-015 | Matching workloads wait indefinitely or start before the required Open Barrier exists | Workload apply and durable barrier publication were ordered inconsistently, creating a startup race between application readiness and the barrier prerequisite | Static-check that the topic Job and Kafka readiness precede barrier publication, and barrier publication precedes Matching workload apply; do not infer readiness from Pod status alone | Publish the barrier before applying the Matching workload and rerun only the Kubernetes phase that was blocked | 2026-08-15 |
| DT-016 | Stable Matching Pods restart after the liveness window even though the process is healthy | The status file is updated only on state transitions, while the metrics file is a periodic heartbeat; using status mtime as liveness made a healthy READY process appear stale | Exercise the liveness probe with fresh metrics plus stale status, stale metrics, and FAILED status before a deployed run | Use the periodic metrics heartbeat for freshness while retaining the status state check; rerun the liveness contract and affected E2E phase | 2026-08-15 |
| DT-017 | BootBuildImage fails with a run-image platform/export error after Docker data relocation | A multi-platform Paketo tag resolved through an image index that the relocated local store could not export for the requested amd64 platform | Before BootBuildImage, verify host architecture, local run-image OS/architecture, and `docker image save --platform linux/amd64` for the exact reference | Use the verified amd64 local recovery reference or flattened image with `IF_NOT_PRESENT`; keep the canonical registry reference unchanged and never use broad Docker prune as repair | 2026-08-15 |
| DT-018 | Worker-stop E2E fails while executing the post-fault helper | The helper Pod was scheduled on the same worker that the test stopped; `restartPolicy: Never` left it Failed, so the harness tried to exec into a completed Pod | For worker-stop, resolve a Ready local-resilience worker different from the fault target and verify the helper Pod is scheduled there before fault injection | Pin the run-owned helper Pod to the unaffected worker and rerun only the worker-stop case | 2026-08-16 |
| DT-019 | The live CDC contract test rejects a valid outbox record as having an unexpected `__debezium.context.*` header | The verifier modeled EventRouter output as only the `id` and configured business header, but Debezium 3.6 source records also carry four standardized connector-context headers | Before encoding an exact header-set assertion, verify the pinned Debezium version's header producer contract and keep an interface regression with all standardized context headers plus malformed-context and unexpected-header cases | Validate the complete known header shape: exact EventRouter/business headers, the expected PostgreSQL task/plugin context, non-empty logical name, and UUID-shaped dynamic run id; do not broadly ignore reserved-prefix headers | 2026-08-21 |
| DT-020 | Image-transport tests, live smoke, or certification disagree about whether `--transport` / `--image-transport` exists | A premature registry-only cutover removed the approved `kind-load` compatibility path while other callers and the staged rollout still depended on it | Keep one deterministic contract for `registry` default plus explicit `kind-load` fallback; when retiring the fallback, treat that as a separate approved interface change and update every caller/test/document together | Restore the transport selector and legacy fallback boundary without weakening registry digest identity, then rerun deterministic contracts and the complete live kind registry smoke | 2026-08-22 |
| DT-021 | Runtime market-data replay persists PostgreSQL state, but the publisher and streamer report `UNKNOWN_TOPIC_OR_PARTITION` for `marketdata.events` | Compose created the topic while the Kubernetes KRaft provisioning Job omitted it, so production-like topology depended on an environment-specific side effect | Require the KRaft manifest contract and live topic preflight to verify `marketdata.events` has 15 partitions, replication factor 3, delete retention, 30-day retention, and minimum ISR 2 before replay | Add the topic to the canonical KRaft provisioning Job, rerun the exact completed provisioning Job or create the topic with its identical policy in a disposable retained namespace, then rerun market-data certification | 2026-08-27 |
| DT-022 | The projection outbox remains pending after `marketdata.events` exists, with `Can't convert value of class [B to StringSerializer` | The output adapter sends exact Protobuf bytes, but the Spring Kafka producer inherited the default string value serializer | Boot the real application context and assert String key plus byte-array value serializers before building or deploying the projection image | Configure the producer value serializer as `ByteArraySerializer`, retain the string key serializer explicitly, rebuild the projection image, and rerun publication through the public stream | 2026-08-27 |
| DT-023 | Gateway close certification exits with `observer_manifest: unbound variable` before creating its Kafka observer | Shared helper preparation coupled the always-required Kafka observer to the optional Matching Event observer through caller-owned global variables | Contract-test Kafka-only helper preparation with `observer_manifest` explicitly unset, and keep Kafka and Matching Event manifest preparation as independent operations | Split helper-manifest preparation by observer responsibility; Gateway close prepares only the Kafka observer while failure certification may prepare both | 2026-08-27 |
| DT-024 | A fresh retained production-like namespace is rejected with `production-like source provenance is missing` | The producer wrote provenance under its selected certification evidence directory, while the dependent runner implicitly resolved a different default directory through a similarly named environment variable | Pass the retained production-like evidence directory explicitly to every dependent certification and contract-test a non-default path without setting the legacy environment variable | Make retained evidence an explicit runner input, validate namespace/source/image provenance before state mutation, and initialize dependent evidence first so preflight rejection still emits `verdict.json` | 2026-08-27 |
| DT-025 | A retained KRaft topic-provisioning Job times out after a broker sandbox/runtime recreation, while the affected broker remains in CrashLoopBackOff with `UnknownHostException` for peer headless-service names | The broker-side runtime was recreated while the Kafka quorum was active; the affected JVM retained a negative peer-DNS result even after EndpointSlice records and `getent` recovered, so provisioning could create early topics but could not complete the retained topic set | Before provisioning and after any node/runtime disruption, resolve each `kafka-{0,1,2}.kafka-headless` name from a broker-side probe and require all three broker startup/readiness probes to pass continuously | Restart only the affected broker Pod after peer DNS is confirmed, preserving its retained PVC; if the scoped restart does not converge, stop and rebuild the canonical cluster through `manage-simplematch-live.sh` rather than changing Kafka data or offsets | 2026-08-31 (redacted: `out/certification/fresh-20260828-0827/kafka-failure/one-broker-loss/matching.commands.topic.txt`) |
| DT-026 | The retained RM-1 verifier Job enters `ImagePullBackOff` and tries Docker Hub for `simplematch/risk-matching-e2e-verifier:local` in a registry-mode namespace | The stable verification manifest names the local compatibility tag, while registry transport publishes only the immutable localhost registry reference; the kind nodes therefore have no matching local tag | Before creating a retained helper Job, compare its image reference with the run's image transport and verify the exact image identity is executable on every eligible node | Load only the image-lock-verified verifier image into the canonical kind nodes, or render the stable helper manifest to the retained immutable registry digest; never pull an unpinned public image or change the verifier semantics | 2026-08-31 (redacted: `out/certification/issue-137-query-20260831-r9-rm1/diagnostics-pods.txt`) |
| DT-027 | RM-1 admission rejects a freshly inserted Account limit as `INSUFFICIENT_AVAILABLE_NOTIONAL` even though the fixture has a large balance | The shell fixture derived its limit date from the host wall clock, but the reservation operation carries an explicit historical certification trading day and Account authority queries that business day | Assert the fixture SQL uses the explicit `--trading-day` argument and inspect the persisted `(account_id, trading_day)` row before submitting the order | Seed only the run-scoped `ACCOUNT/*` limit on the command's explicit trading day, then rerun the typed verifier; do not weaken Risk validation or insert a second service-owned state path | 2026-08-31 (redacted: `out/certification/issue-137-query-20260831-r4-rm1/verifier-verdict.json`) |
| DT-028 | A fresh production-like run times out at `kafka-connect` rollout after five minutes, while the connector Pods later become Ready without restarts | The first kind run had to pull and unpack the pinned roughly 1 GB Debezium image from Quay; the repository's 300-second rollout wait expired before the cold pull completed, so the timeout was an image-cache/startup-window failure rather than a connector application failure | Before applying workloads, verify the exact pinned Debezium image is available and executable on every eligible kind worker, and record whether the run is cold-cache or warm-cache; treat a cold pull that exceeds the rollout window as a precondition failure | Let the exact pinned image pull complete or pre-pull it through the verified worker runtime, then rerun the retained run without changing Kafka data, offsets, or connector semantics; preserve the timeout diagnostics | 2026-08-31 (redacted: `out/certification/issue-137-query-20260831-desktop-r1/logs/kubernetes-risk-outbox-connector.log`) |
| DT-029 | A clean Desktop kind cluster repeatedly places kube-controller-manager and kube-scheduler in CrashLoopBackOff, or namespace cleanup remains pending, while the application namespace is empty | Docker Desktop VM I/O stalls etcd's linearizable reads and lease transactions; the control-plane components lose their leases and fail their local probes even though etcd may later report Ready, so this is an environment stability failure rather than an application workload result | Before applying deployment resources or starting cleanup, require controller-manager, scheduler, and etcd to be Ready with unchanged restart counts across a bounded stability window, verify `/readyz?verbose`, and inspect recent control-plane events for lease timeouts or probe failures | Preserve the control-plane and etcd diagnostics, allow a bounded recovery, then rebuild only the canonical cluster through `manage-simplematch-live.sh` when the failure repeats; do not force-finalize namespaces, edit Kafka data, or reinterpret a run started during the unstable window | 2026-08-31 (redacted: `out/certification/issue-137-query-20260831-desktop-r4/diagnostics/etcd-tail.log`, `out/certification/issue-137-query-20260831-desktop-r6-preflight/controller-manager-recheck.log`) |
| DT-030 | RM-1 fixture setup reports that the mounted Market Reference has no eligible final-price instrument even though the immutable ConfigMap contains priced instruments | Kubernetes serializes the binary `market_reference.json` key under `binaryData`, while the fixture orchestrator read only `.data["market_reference.json"]` and passed null to `fromjson` | Before creating the verifier Job, decode the exact ConfigMap through the shared `decode_configmap_file` seam and exercise both Kubernetes ConfigMap storage forms in the RM-1 contract test | Reuse the shared binaryData/data decoder and parse the decoded artifact JSON; do not rewrite the immutable artifact or relax Matching eligibility checks | 2026-09-01 (redacted: `out/certification/issue-137-query-20260901-final/matching-fixture/buy/market-reference-configmap.json`) |
| DT-031 | Query-service certification stops at `capture baseline` after a public RM-1 BUY/SELL fixture leaves QuickFIX and Account quarantine history | The fixture sent Matching commands without a QuickFIX owning session, and the BUY account lacked a position row required when its trade fill was applied; the fail-closed consumers correctly preserved both failures | Require execution-backed fixtures to enter through the public FIX seam, provision every Account authority row required by both trade legs, wait for both FIX delivery intents and `RESERVATION_STATUS_APPLIED`, then assert a clean critical-consumer baseline | Use two public FIX orders with explicit BUY/SELL side support, seed only run-scoped limit/position authority, and keep quarantine handling fail-closed; never delete quarantine rows or weaken consumer validation | 2026-09-01 (redacted: `out/certification/issue-137-query-20260901-query-final/critical-before.json`) |
| DT-032 | Query-service certification cannot start the public FIX fixture because `port-forward.log` is redirected under a missing `matching-fixture/fix` directory | The fixture created baseline, open, and submission evidence directories but the shared port-forward helper assumed every caller had already created the parent directory for its log path | Run the execution-fixture contract with a fresh evidence root and an absent `fix` directory before deployment; require the helper to create the log parent before starting a port-forward | Let the shared `start_port_forward` initialize the exact log parent, preserving the caller's evidence root and failure diagnostics; do not pre-create arbitrary paths or ignore the redirection error | 2026-09-01 (redacted: `out/certification/issue-137-query-20260901-query-final2/verdict.json`) |
| DT-033 | Query-service execution fixture retries Gateway observations against `127.0.0.1:80` and cannot capture Kafka log-end positions | The fixture started FIX and Gateway port-forwards but never initialized the shared in-cluster Kafka observation adapter, leaving its port variable empty; cleanup also had no ownership path for the missing observer Pod | Before opening the Gateway, require the fixture contract and runtime path to initialize the pinned Kafka observer manifest and adapter, then capture a non-empty forwarded port; cleanup must stop the adapter and delete its run-owned Pod | Reuse the critical-consumer Kafka observation interface for setup and teardown, preserving source/image provenance and observation failure semantics; do not default an empty port to port 80 or skip Kafka stability evidence | 2026-09-01 (redacted: `out/certification/issue-137-query-20260901-query-final4/verdict.json`) |
| DT-034 | A source-aligned production-like run stops during the Matching image build with `rpc error: code = Unavailable desc = error reading from server: EOF` | Docker Desktop's BuildKit server was shut down while the vcpkg/Protobuf build was streaming; the application build had not produced a source or test failure | Before a long image build, require `docker info` and the selected BuildKit builder to be healthy, and keep Docker Desktop running until the command exits; classify a daemon shutdown as an environment precondition failure | Reopen Docker Desktop, wait for the Docker API and builder health checks to pass, then rerun the complete source-aligned run with a new evidence directory; do not promote partial build output to deployment evidence or alter source/build semantics | 2026-09-01 (redacted: `out/certification/issue-137-query-20260901-prod-final4/logs/local-image-build/matching.log`, Docker Desktop build log) |
| DT-035 | Query-service public FIX fixture receives `NEW_ORDERS_PAUSED` even though the Gateway `/operations/open` response was accepted | The fixture opened the gate, then spent longer than the configured five-second `stale-status-after` window starting the Gradle FIX client; the Gateway's fail-closed monitor correctly paused new orders after the last readiness observation aged out | Hold the FIX client at its ready barrier, submit fresh Gateway observations after that barrier, and release the client immediately; assert this order in the execution-fixture contract and retain the Gateway audit row | Move the fresh open sequence into each BUY/SELL submission immediately before release; do not retry the rejected order or relax the stale-status protection | 2026-09-01 (redacted: `out/certification/issue-137-query-20260901-query-final5/matching-fixture/open/gateway-open-response.json`, `matching-fixture/buy/fix/submit.json`, and `quickfix_gateway.gateway_operation_audit`) |
| DT-036 | Risk-service and QuickFIX Flyway Jobs are first recorded as `OOMKilled` under the local 2 GiB container limit, then succeed on the Job retry with the same image | A cold Gradle/Kotlin process can exceed the container cgroup peak even with a 1 GiB heap and in-process compiler; the sequential migration boundary prevents application contention but did not bound Gradle task workers | Rendered local Flyway Jobs must expose the bounded JVM, in-process compiler, and a positive `SIMPLEMATCH_GRADLE_MAX_WORKERS` value; inspect every failed Job Pod termination reason before publishing a migration PASS | Pass `--max-workers=1` through the shared Flyway runner and keep the existing backoff/complete-job proof; never treat an OOM retry as a source or schema failure | 2026-09-01 (redacted: retained namespace `simplematch-local-cert-20260901-063222-16645`, Pods `risk-service-flyway-fl2qj` and `quickfix-gateway-flyway-58462`) |
| DT-037 | Matching owner `matching-0` exits during startup with `unable to query Matching Kafka committed offset: NOT_COORDINATOR` and is restarted before becoming Ready | The broker had accepted the topic/barrier setup but its group coordinator was not yet stable when the direct-assignment consumer queried committed offsets; the current Matching startup treats that transient metadata error as fatal | Before applying Matching, verify all Kafka brokers and headless peer names are Ready, then inspect every Matching Pod's restart count and previous log for coordinator errors; keep the startup result distinct from a later Ready state | Preserve the failed-Pod evidence, allow the bounded Kubernetes restart after Kafka metadata converges, and do not edit offsets or data; a client-side retry policy remains a separate follow-up if the race repeats | 2026-09-01 (redacted: retained namespace `simplematch-local-cert-20260901-063222-16645`, `matching-0 --previous`) |
| DT-038 | Query active-liveness can release a prepared public FIX order after Gateway readiness has aged out | The runner opened Gateway before stopping Query and collecting the quiescent isolation samples; that bounded probe can exceed the five-second `stale-status-after` window, so the fail-closed monitor may pause new orders between the accepted open response and FIX release | Keep the FIX client at its ready barrier, collect fresh observations after the outage/isolation barrier, and assert that the final open sequence precedes `release_fix_submit_client` in the active-liveness contract | Re-open Gateway immediately before releasing the prepared FIX client while Query remains at zero Pods; retain the accepted response and do not retry a rejected order or weaken stale-status protection | 2026-09-01 (static audit: `scripts/end-to-end/query-service/lib/active-liveness.sh` and `scripts/end-to-end/query-service/tests/active-liveness-contract.sh`) |
| DT-039 | Query public FIX fixture quarantines a valid `matching.events` record with `value too long for type character varying(36)` while creating a delivery intent | The external contract permits a 64-character `ClOrdID` and derives `OrderID = O-<ClOrdID>`, but the WAL/schema boundary treated `order_id` as a UUID-sized value; the production fixture therefore produced a 38-character OrderID that the `VARCHAR(36)` ledger rejected | Contract-test the maximum `O-<ClOrdID>` length through WAL construction and Flyway metadata before the next Query run, then inspect the QuickFIX quarantine table after fixture setup | Allow only the derived 66-character OrderID in `WalOrderReference`, apply the versioned V3 column widening, and keep the consumer fail-closed; never delete the quarantine row or truncate the identity | 2026-09-01 (redacted: `out/certification/issue-137-query-20260901-query-racefix-r2/matching-fixture/buy/verdict.json` and retained `quickfix_gateway.matching_consumer_quarantines`) |
| DT-040 | Query public FIX cross-fixture stops before the SELL order with `baseline contains quarantine history, pending FIX delivery, or active orders` | The fixture intentionally leaves the BUY order `RESTING` so the SELL order can execute against it, but the generic Gateway observation guard required zero active Matching orders before every FIX release | Keep the default observation expectation at zero and contract-test the cross-fixture SELL path with exactly one expected active order before release | Pass an explicit expected active-order count only for the SELL fixture; retain all quarantine, pending-intent, freshness, and consumer-progress guards | 2026-09-01 (redacted: `out/certification/issue-137-query-20260901-query-orderid/matching-fixture/sell/baseline/observation-query-fixture-SELL-1-attempt-1/consumer-state.json`) |
| DT-041 | Query public FIX matched fixture receives trade reports but the prepared client times out waiting for a terminal report | Both immediate and durable FIX mappers emitted internal filled status `2` as `ExecType`; the FIX 4.4 dictionary accepts `F` for `TRADE`, so QuickFIX rejected both trade reports before application delivery | Run a public BUY/SELL match through the repository dictionary and assert `ExecType=F`, `OrdStatus=2`, and no `IncorrectTagValue` rejection before the next Query run | Map fills to `ExecType=F` and `OrdStatus=2` in both matching-event paths; keep the dictionary validation strict and never accept an invalid wire value to make the fixture pass | 2026-09-01 (redacted: `out/certification/issue-137-query-20260901-query-f3678be/matching-fixture/sell/client-state/log/FIX.4.4-CLIENT-SIMPLEMATCH.event.log`) |
| DT-042 | Query-service certification times out on the first public read after Redis is scaled to zero instead of observing the PostgreSQL fallback | The custom Lettuce connection factory used the library's reconnect/command defaults; an empty Redis Service endpoint therefore held a cache read past the runner's 20-second HTTP bound before `QueryReadController` could handle the failure | Inspect the Query Redis client configuration for positive bounded connect and command timeouts before injecting the Redis outage, then require the fallback request to finish within the public-read timeout | Bind explicit Query Redis timeouts and pass them to Lettuce socket and command configuration; preserve Redis as a disposable acceleration layer and keep the durable read path unchanged | 2026-09-01 (redacted: `out/certification/issue-137-query-20260901-final-query/verdict.json`) |
| DT-043 | Corrected certification source-preflight rejects untracked `build-logic/.kotlin/errors/errors-*.log` and `.salive` files after a Gradle test | The Kotlin compiler daemon writes local diagnostics and liveness markers into a repository-local generated directory when its user directory is not writable; the runtime provenance pathspec correctly rejected the untracked files but the repository had no ignore rule for this build state | Before certification, run the provenance helper after Gradle tasks and verify generated `.kotlin/` state is ignored while a source/config/deployment probe remains rejected | Ignore only recursive `.kotlin/` compiler state and add a provenance contract probe for it; preserve fail-closed checks for all non-generated runtime inputs and do not delete source files broadly | 2026-09-01 (redacted: `build-logic/.kotlin/errors/errors-1788269342090.log`) |
| DT-044 | Query-service certification fails while capturing critical-path health with `jq: Argument list too long` after Query outage isolation starts | The collector passed the complete JSON list of 15 Matching Pods through `jq --argjson`, so valid but large Pod metadata exceeded the host `execve` argument limit before health evidence could be written | Run the critical-path isolation contract with a deliberately large Pod JSON payload and require the collector to materialize the input through a file-backed `jq --slurpfile` path | Write each `kubectl get pods -o json` result to a run-owned temporary evidence file, read it with `--slurpfile`, and remove it on success or failure; preserve all existing replica, readiness, restart, and topology checks | 2026-09-01 (redacted: `out/certification/issue-137-query-20260901-final-corrected2-query/critical-query-isolation-probe.samples`) |
| DT-045 | Query-service certification rejects the quiescent isolation probe after a healthy first sample because the collector elapsed beyond the nominal five-second value | The probe treated its sample count and one-second interval as a hard wall-clock deadline, but one complete Kubernetes/Kafka/evidence observation can legitimately take nearly five seconds before the next sample starts; the fail-closed health checks themselves were healthy | Run the isolation contract with delayed clock/observation handling and require complete requested samples plus actual elapsed evidence; statically verify that active liveness reopens fresh Gateway observations before FIX release | Keep the per-command Kubernetes timeout and all health/topology/offset invariants, but remove the aggregate hard deadline and record true elapsed time; do not weaken query-Pod absence, critical-consumer, Matching fleet, UID, restart, or monotonic-offset checks | 2026-09-01 (redacted: `out/certification/issue-137-query-20260901-reviewfirst-query/verdict.json`) |
| DT-046 | Query-service active-liveness reaches the exact Matching Event and all downstream projections, but the deterministic verdict reports `activeProcessingLiveness=NOT_PROVEN` | The in-cluster observer persisted the matched record offset but omitted the configured Kafka seek lower bound, while the verdict correctly requires evidence that the observed event offset is at or after that bound | Run the observer unit contract and assert the serialized `matching-event-observation.json` contains the requested `startOffset` alongside the matched offset before the next retained Query gate | Carry the observer's requested start offset into its evidence record and JSON output; preserve the verdict's `event.offset >= startOffset` check and all command/order/event identity checks | 2026-09-02 (redacted: `out/certification/issue-137-query-20260902-timingfix-query/verdict.pending.json`) |
| DT-047 | Query-service active-liveness records a valid market-data projection, but the deterministic verdict reports `activeProcessingLiveness=NOT_PROVEN` because market-data progress cannot be compared with the observed event offset | The critical-consumer evidence collector selected only `partition_id` and `recovery_state` from the market-data progress table, while the verdict contract correctly requires `last_processed_offset >= matchingEvent.offset` | Run the evidence collector contract after every schema or verdict change and require the market-data progress projection to expose `partition_id`, `last_processed_offset`, and `recovery_state` before fault injection | Include `last_processed_offset` in `marketDataProgress` while preserving the READY-state and partition/offset comparison; do not weaken the active-liveness verdict or infer progress from the top-level projection summary | 2026-09-02 (redacted: `out/certification/issue-137-query-20260902-start-offset-query/verdict.pending.json`) |
| DT-048 | Redis worker-loss diagnostic reports an unexpected marker after recovery even though the new Pod correctly lost its disposable state | Non-interactive `redis-cli GET` returns an empty line for a missing key in this container image, while the diagnostic only recognized the interactive `(nil)` spelling | Contract-test the Redis marker classifier with the exact expected value, an empty response, `(nil)`, and an unexpected response before injecting a worker fault | Treat only the exact marker as present and the two known missing-key responses as absent; keep every other response fail-closed and do not make Redis state durable | 2026-09-05 (redacted: `out/resilience/dependencies-154-155-20260905-r2/redis-fixed/redis.json`) |

## How to add a lesson

Add a row only when the failure is repeatable or materially risky. Each row must state:

- the observable symptom;
- the concrete root cause, separated from unrelated defects;
- a preflight check that can catch it before deployment or fault injection;
- a safe fix that preserves authoritative data and fail-closed behavior;
- the date and a redacted evidence source.

Do not put credentials, bearer tokens, Secret values, raw FIX payloads, complete account payloads, or
unredacted environment variables in this file or its evidence links. Review this table when changing
deployment scripts, certification profiles, image/build workflows, or cleanup behavior, and remove
or revise entries when the prevention check no longer matches the implementation.

## Disk hygiene boundary

Disk cleanup is an inventory operation, not an availability shortcut:

- Preserve running containers, the canonical kind cluster, images loaded into it, active Compose
  volumes, and caches required by a running build or test.
- Compare image IDs/content, not tags alone. If duplicate tags point to one image and no active
  resource references the older tag, retain the newest tag and remove the stale alias.
- Treat generated `out/`, `build/`, `vcpkg_installed/`, Gradle, and Docker builder cache as
  disposable only after confirming the current or next requested workflow does not rely on them.
- After cleanup, record what was removed, what was retained, and the measured space change.
